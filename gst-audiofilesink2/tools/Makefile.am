bin_PROGRAMS = gst-audiofilesink2

# sources used to compile this plug-in
gst_audiofilesink2_SOURCES = gstaudiofilesink2.c

# compiler and linker flags used to compile the program, set in configure.ac
gst_audiofilesink2_CFLAGS = $(GST_CFLAGS)
gst_audiofilesink2_LDADD = $(GST_LIBS)

